//home模块的仓库
const state = () => ({
    
})

//mutations是唯一修改state的地方
const mutations={
    
}

//action是用户处理派发action的地方，可以书写异步语句、逻辑的地方
const actions={
    
}

const getters={}
export default {
    state,
    mutations,
    actions,
    getters
}